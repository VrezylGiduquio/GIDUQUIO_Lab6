{
  "name": "giduquio-lab6",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix node-mysql-api install --ignore-scripts && npm run backend:build",
    "start": "npm --prefix node-mysql-api start",
    "frontend": "cd frontend && npm start",
    "frontend:build": "cd frontend && npm run build",
    "frontend:test": "cd frontend && npm test",
    "backend": "cd node-mysql-api && npm run dev",
    "backend:build": "cd node-mysql-api && npm run build",
    "backend:start": "cd node-mysql-api && npm start"
  }
}
